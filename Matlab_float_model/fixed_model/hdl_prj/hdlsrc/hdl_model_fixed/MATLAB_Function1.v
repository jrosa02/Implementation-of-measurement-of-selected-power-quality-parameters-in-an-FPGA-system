// -------------------------------------------------------------
// 
// File Name: hdl_prj\hdlsrc\hdl_model_fixed\MATLAB_Function1.v
// Created: 2024-12-20 21:02:02
// 
// Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
// 
// -------------------------------------------------------------


// -------------------------------------------------------------
// 
// Module: MATLAB_Function1
// Source Path: hdl_model_fixed/Subsystem1/BUF_MIN_MAX_AVG/MATLAB Function1
// Hierarchy Level: 2
// Model version: 1.105
// 
// -------------------------------------------------------------

`timescale 1 ns / 1 ns

module MATLAB_Function1
          (clk,
           reset,
           enb,
           input_data,
           reset_1,
           max_out);


  input   clk;
  input   reset;
  input   enb;
  input   signed [11:0] input_data;  // sfix12_En11
  input   reset_1;
  output  signed [11:0] max_out;  // sfix12_En11


  reg signed [11:0] max_out_1;  // sfix12_En11
  reg signed [11:0] current_max;  // sfix12
  reg signed [11:0] current_max_next;  // sfix12_En11
  reg signed [11:0] current_max_temp;  // sfix12_En11


  always @(posedge clk)
    begin : MATLAB_Function1_1_process
      if (reset == 1'b1) begin
        current_max <= 12'sb110000000000;
      end
      else begin
        if (enb) begin
          current_max <= current_max_next;
        end
      end
    end

  always @(current_max, input_data, reset_1) begin
    current_max_temp = current_max;
    // input_data: Input data stream (e.g., 16-bit integer)
    // reset: Reset signal (active high)
    // Persistent variable to hold the current maximum value
    // Initialize persistent variable if it's the first time the function is called
    // On reset, set the maximum value back to a very small value
    if (reset_1) begin
      current_max_temp = 12'sb110000000000;
      // Set to the smallest value for fixed-point
    end
    else begin
      // Update the current maximum on each clock cycle
      if (input_data > current_max) begin
        current_max_temp = input_data;
      end
    end
    // Output the current maximum value
    max_out_1 = current_max_temp;
    current_max_next = current_max_temp;
  end



  assign max_out = max_out_1;

endmodule  // MATLAB_Function1

